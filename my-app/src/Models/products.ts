export interface ProductModel {
  productId: number;
  productName: string;
  productPrice: number;
  productImage: string;
  productBrand: string;
  productRating: number;
  featuredProduct: boolean;
}
